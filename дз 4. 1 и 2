1. Упражнение на генераторы
from math import factorial

# 1. Список квадратов первых 10 натуральных чисел
squares = [x**2 for x in range(1, 11)]
print("1.", squares)

# 2. Словарь дней недели и их порядковых номеров
days = ["Понедельник", "Вторник", "Среда", "Четверг", "Пятница", "Суббота", "Воскресенье"]
days_dict = {day: index+1 for index, day in enumerate(days)}
print("2.", days_dict)

# 3. Множество тегов библиотек в нижнем регистре из исходного списка
libraries = ["Django", "FastAPI", "Numpy", "PYTHON", "Pandas", "FASTAPI", "Python", "random"]
tags = {lib.lower() for lib in libraries}
print("3.", tags)

# 4. Список только четных чисел из исходного списка
numbers = [1, 3, 4, 87, 98, 15, 7, 4]
even_numbers = [num for num in numbers if num % 2 == 0]
print("4.", even_numbers)

# 5. Словарь чисел от 1 до 5 и их факториалов
factorials = {n: factorial(n) for n in range(1, 6)}
print("5.", factorials)


2. Генератор простых чисел"
import math

def prime_numbers():
    num = 2
    while True:
        is_prime = True
        sqrt_num = math.isqrt(num)  # целая часть квадратного корня
        for divisor in range(2, sqrt_num + 1):
            if num % divisor == 0:
                is_prime = False
                break
        if is_prime:
            yield num
        num += 1

# Пример использования: вывод первых 10 простых чисел
gen = prime_numbers()
for _ in range(10):
    print(next(gen))
